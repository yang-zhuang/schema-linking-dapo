# 评估系统依赖包

# 核心依赖
torch>=1.9.0
transformers>=4.20.0
pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.11.0

# 数据处理
jinja2>=3.0.0
tqdm>=4.60.0

# 可视化
plotly>=5.0.0
kaleido>=0.2.0  # 静态图片导出

# 推理加速 (可选)
vllm>=0.2.0  # VLLM推理引擎

# 量化支持 (可选)
bitsandbytes>=0.39.0
peft>=0.4.0  # LoRA支持

# API调用 (可选)
requests>=2.25.0

# JSON处理
ujson>=5.0.0  # 更快的JSON处理

# 日志和配置
pyyaml>=6.0
colorlog>=6.0.0

# 类型检查
typing-extensions>=4.0.0

# 测试依赖 (开发用)
pytest>=7.0.0
pytest-cov>=4.0.0